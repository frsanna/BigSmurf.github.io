cartella apps:

  - codice che tira fuori la lista di tutte le cartelle;
  - immagine di default delle app;
  - all'interno di ogni cartella ci dovra essere:
    -- HTML della pagina (index.html);
    -- me.json -> descrittore della pagina nel seguente formato:
        {
          id: "nome della pagina",
          desc: "breve descrizione delle sue funzionalità"
        }
    -- icona/immagine dell'app da utilizzare come bottone del link
